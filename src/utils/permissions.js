/**
 * iOS sensor permission flow. Phase 0 stub — Phase 1 ports
 * requestSensorPermission() from avatar-cam.js. The DeviceMotionEvent
 * and DeviceOrientationEvent requestPermission() calls MUST happen
 * inside a user-gesture handler on iOS 13+.
 */

export async function requestSensorPermission() {
  throw new Error('requestSensorPermission() not implemented yet (Phase 0 stub).');
}
