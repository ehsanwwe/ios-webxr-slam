/**
 * Holds engine session state and orchestrates the permission flow.
 * Phase 0 stub — Phase 1 will move the working requestSensorPermission()
 * gesture-context logic here.
 */
export class SessionManager {
  constructor() {
    this.active = false;
  }

  async begin() {}

  async end() {}
}
