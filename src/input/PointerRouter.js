/**
 * Routes raw pointer events to either the gesture recognizer or to
 * consumer-supplied handlers (e.g. for hit-testing 3D buttons in the
 * scene). Phase 0 stub.
 */
export class PointerRouter {
  constructor() {}

  bind(_element) {}

  unbind() {}
}
